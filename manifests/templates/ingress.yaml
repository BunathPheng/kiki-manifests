---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kiki                  # Ingress name
  labels:
    app: kiki
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "kiki.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kiki               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
